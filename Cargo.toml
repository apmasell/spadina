[workspace]
resolver = '2'
members = [
    'core',
    'client',
    'console-client',
    'server',
]

# generated by 'cargo dist init'
[profile.dist]
inherits = "release"
debug = true
split-debuginfo = "packed"

